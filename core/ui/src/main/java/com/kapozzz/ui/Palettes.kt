package com.kapozzz.ui

internal val baseLightPalette = Colors(
    primary = Pink,
    onPrimary = White,
    secondary = LightPink,
    background = White,
    onBackground = Black,
    container = LightGray,
    onContainer = DarkGray,
    outline = Gray,
    error = Red,
    firstTint = Orange
)